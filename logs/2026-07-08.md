# 2026-07-08

比 `docs/commit-log.md` 更詳細的工作記錄：每個動作記下當時的請求、實際做了什麼、以及為什麼這樣做。commit hash 對照 `git log`。

---

## 15:52 — 建立 design/ 資料夾 [`7f5f3d4`]

**請求：** 把目前的視覺與玩法設計歸納寫一版到 design 資料夾下。

**做了什麼：** 讀完整個 `index.html`（CSS 變數色彩系統、UR/SSR/SR/R/N 權重與保底機制、評分公式、地球儀投影渲染邏輯），寫成 `design/DESIGN.md` 作為現況設計文件。

---

## 16:01 — 進站滑動選國籍 + 多世輪迴 PRD [`fed617d`]

**請求：** 一進站要能滑動選自己的現世國籍；接著貼了團隊群組討論整理的「多世輪迴鏈」PRD，要求包裝寫進 design 資料夾。

**做了什麼：**
- 新增 `#onboardOverlay`：重用既有 202 筆國家資料，用 CSS `scroll-snap` 做原生橫向滑動選擇，選完存 localStorage，下次進站不再重複詢問。
- 把 PRD 整理成 `design/prd-samsara-chain.md`，並指出一個衝突：PRD 的 FR-6 說「起點應完全隨機、不開放選國籍」，但同一天才做的滑動選國籍功能字面上牴觸。提出解法：保留功能，重新定位成 FR-5 的「終局收斂目標」而非起點——這個決策後續在使用者回覆「A」後正式拍板。

---

## 16:08 — 地球拖曳旋轉、滾輪選國籍、台灣排第一 [`0ff370b`]

**請求：** 地球要能按住拖曳旋轉；選國籍滑動清單滾輪沒作用；把台灣排到前面方便選。

**做了什麼：**
- 用 Pointer Events（`pointerdown`/`pointermove`/`pointerup`）加上滑鼠與觸控拖曳旋轉地球，拖曳時關閉自動旋轉。
- 選國籍清單加 `wheel` 事件監聽，把垂直滾輪 deltaY 轉成水平 `scrollLeft`。
- 調整選國籍清單的顯示順序，把台灣移到第一個（不影響底層 `ALL_COUNTRIES` 陣列順序，只調整渲染順序）。

---

## 16:23 — 修正選國籍高亮判斷用錯座標系 [`367f376`]

**背景：** 使用者回報「選過一次國籍後，重新打開選擇畫面，怎麼滑都選不到別的國家」。

**做了什麼：** 用瀏覽器 DevTools 現場診斷，發現 `updateActiveChip()` 拿 `chip.offsetLeft`（相對於固定定位的 `.onboard` 外層容器）去跟 `track.scrollLeft`（相對於清單自己）比較，兩個座標系不是同一個參考點，只有在 `scrollLeft=0` 時碰巧算對。改用 `getBoundingClientRect()` 算相對位置，不受 `offsetParent` 影響。

---

## 16:32 — 十連變成十世輪迴鏈，Phase 1 上線 [`c161f28`]

**背景：** 使用者針對 `design/prd-samsara-chain.md` 說「幫我根據這個PRD開始實作」。因為 PRD 有兩個關鍵開放問題會影響核心玩法，先用 AskUserQuestion 跟使用者確認：世數要固定還是浮動（選了固定 10 世）、保底機制怎麼算（選了沿用，10 世＝10 抽保底週期）。

**做了什麼：** 把既有「十連」按鈕重新定義成一次完整輪迴鏈：
- 點擊後跳出起始西元年滑桿（原生 `<input type="range">`，1776–2010）。
- 確認後跑 10 次原本的 `rollOne()`，額外算一個西元年（上一世年份 + 該等級平均壽命 ±20%），保底機制完全沿用不改。
- 第 10 世年份強制收斂 2026（不改動抽到的國家/稀有度）。
- 結尾彈出反思式結語畫面。

---

## 16:39 — 結語畫面文案微調 [`7af70d7`]

**請求：** 使用者直接給了想要的結語文案（「這一世 你還記得嗎 你一直想成為的人…」），並要求「十世流轉」後面該換行、破折號改成刪節號。

**做了什麼：** 套用使用者提供的原文，調整換行與標點。

---

## 16:49 — 輪迴回顧畫面 + 第 10 世強制收斂現世國籍 [`6a7ba43`]

**請求：** 十連結果應該先停留讓使用者看完十世的細節（去了哪裡、活多久、做什麼職業、發生了什麼事）才進結語畫面；另外「最後一個國家必定是一開始選的國家」。

**做了什麼：**
- 新增 `#chainReviewOverlay`「輪迴回顧」：可捲動清單列出每世的世數、年份區間、國家、隨機配的職業（新增 `OCCUPATIONS` 依等級各 4–5 個稱謂）、重用該國既有 `reason` 文案後半段的敘事。看完按按鈕才進結語畫面。
- 第 10 世改為直接用玩家選的 `selectedHome` 組出結果，不再呼叫 `rollOne()`，但仍手動遞增 `sincePity`/`stats`/`historyData`，確保保底與統計口徑不受影響。

---

## 17:06 — 世數改浮動，用真實壽命累加到出生年 [`3967b5d`]

**背景：** 使用者回報兩個問題：(1) 十連小卡的年份會衝過 2026 又被拉回來，時間邏輯不合理；(2) 結語畫面顯示的國家跟回顧清單第 10 世的國家對不起來。開始修時間分佈算法（等比例縮放湊出剛好 10 世收斂 2026），但使用者中途提出不同想法：「各國有平均年齡，隨機 ±20% 活那一世，然後累加到達使用者自己的出生年就停」——用 AskUserQuestion 確認是否要把「固定 10 世」改成「浮動世數」，確認後照此重做。

**做了什麼：**
- 進站選國籍畫面新增「你今年幾歲？」原生滑桿，回推出生年（`2026 - age`），跟國籍一起存 localStorage。
- 起始年滑桿的上限動態設為 `min(2010, 出生年-1)`，避免選到比自己出生年還晚的起始年。
- 輪迴鏈邏輯改成：從起始年開始不斷用真實平均壽命（±20%，不縮放）累加投胎，直到年份到達玩家出生年為止（世數因此浮動，不再固定 10 世）；最後一世固定是玩家的現世國籍＋真實年齡。
- 回顧清單每一世補上「活了幾歲」；捲軸改深色主題；結語按鈕間距加大；文案拿掉寫死的「十連/十世」。

---

## 17:19 — 職業依時代變化、調整起始年範圍 [`a3c169a`]

**請求：** 職業要更複雜、要符合不同年代該有的職業；輪迴鏈的起始年下限要調整得比較合理，讓十世輪迴變成常見結果。

**做了什麼：**
- 把原本單一套跨時代共用的職業列表，改成 `OCCUPATIONS_BY_ERA`：依年份分成 5 個時代（1500 年前、1500–1750 大航海時代、1750–1900 工業革命、1900–1980 近代、1980–2026 當代），每個時代各自維護一份 5 等級的職業稱謂表，用該世的起始年份查表。
- 起始年滑桿下限從 1776 拉大到 1000、預設值改成 1500：用各等級真實平均壽命的加權平均（約 52.4 年／世）反推，1500 年起跳、玩家預設 25 歲（出生年 2001）大約需要 501 年跨度，換算下來大約落在 9～10 世，讓「十世輪迴」變成常見結果而不是極端值。
- 本機測試確認：同一組預設值（台灣、25 歲、起始 1500 年）跑出剛好 10 世，且職業隨時代正確變化（1500 年代藥劑師、1800 年代學校教師、2001 年軟體工程師）。
